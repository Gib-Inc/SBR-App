Stick with the current Direct GHL API integration for V1. Do NOT switch to a webhook-based pattern right now. Instead, I want you to harden and finish the existing direct API path and prepare it for future logging:

Confirm and finalize the PO → GHL send flow

Use the existing createOrFindContact() to ensure the supplier is created/found in GHL before sending.

Use sendEmail() and/or sendSMS() to send the PO via GHL based on the user’s “Send via Email / SMS” choice in the Create PO dialog.

Make sure this uses the subject + body generated by the LLM (from the final confirmation step in the PO modal), not a hard-coded template.

Persist send results for audit + future “Logs” tab

Extend the PurchaseOrder model to store: lastSendChannel (email/sms), lastSendStatus (success/failed), lastSendTimestamp, and lastSendMessageId (whatever ID GHL returns for email/SMS).

After calling sendEmail() / sendSMS(), save these fields on the PO record.

If the send fails, store a failure status and error message so we can see it later and retry.

Wire this to the future AI Logs system (no new UI yet)

Create a small AuditLog table + service (if it doesn’t already exist) with fields like: timestamp, actorType (system/user), eventType (e.g. PO_SENT_GHL_EMAIL, PO_SENT_GHL_SMS), purchaseOrderId, supplierId, and a details JSON field.

Every time we send a PO via GHL (success or failure), write an audit log entry using this service.

Do NOT build the Logs UI yet; just make sure all events are being written correctly so we can surface them later in the AI Agent “Logs” tab.

Keep webhook path strictly V2

Leave the existing gohighlevelInboundWebhookUrl setting and any helper stubs in place, but do not wire them up yet. They should remain unused in V1.

Comment the code clearly where the webhook path would be plugged in later so we can add an alternative “Send via Webhook” mode in V2 without refactoring the current direct API implementation.

Please implement these changes end-to-end, update any relevant TypeScript types and schema files, and run whatever tests you have so the Direct API path is production-ready for V1.