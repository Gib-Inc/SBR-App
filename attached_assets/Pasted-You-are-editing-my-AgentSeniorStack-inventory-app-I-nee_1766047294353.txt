You are editing my “AgentSeniorStack” inventory app. I need you to fully wire QuickBooks webhooks, including a configurable “webhook verifier token” in the QuickBooks settings UI, and a POST endpoint that verifies incoming webhook calls from Intuit.

High-level goals:
- Add a “QuickBooks Webhook Verifier Token” field to the QuickBooks config so I can paste the token from the Intuit Developer portal and update it in the future.
- Store and use that token server-side when validating webhooks.
- Create a POST route at `/api/quickbooks/webhooks` that:
  - Verifies the HMAC signature using the verifier token and the raw request body, according to Intuit’s docs.
  - Safely rejects invalid signatures.
  - For now, logs the event payload in a structured way and returns 200 OK so we can confirm the connection works.
- Keep the implementation consistent with the existing integration patterns you already created for GHL, Shippo, SendGrid, etc.

Please implement the following changes step by step, reusing existing patterns wherever possible:

1) DATA MODEL / CONFIG

1.1) Find the data model / config structure you are already using for QuickBooks. There should already be some place that stores things like the QuickBooks client ID / client secret / realm ID or similar (for example a `QuickBooksConfig`, `integration_quickbooks`, `DataSourceConfig` row, or a similar structure).

1.2) Add a new nullable string field to that model for the webhook verifier token, named **`webhookVerifierToken`**. For example (adjust to match the actual ORM/schema in this project):

- If Prisma:
  - Add to the relevant model:
    - `webhookVerifierToken String?`
  - Create and run the appropriate migration.

- If you are using another ORM / schema system, add an equivalent optional string field.

1.3) In the server-side config loader for QuickBooks (whatever function/class reads QuickBooks settings), make sure that:
- It reads `webhookVerifierToken` from the DB.
- Optionally, if the DB value is empty, it can fall back to an environment variable named `QUICKBOOKS_WEBHOOK_VERIFIER_TOKEN`.
- Prefer the DB value over the env var so that changes made from the settings UI take effect without redeploy.

Expose this value in a type-safe way (e.g., `quickbooksConfig.webhookVerifierToken`) so we can access it from the webhook route.

2) SETTINGS UI – QUICKBOOKS CONFIG

2.1) Find the QuickBooks settings / configuration UI screen (the screen where I connect QuickBooks or configure its keys). This might be under “Settings → Integrations → QuickBooks” or similar.

2.2) On that QuickBooks config card/page, add a new input field:

- Label: **“QuickBooks webhook verifier token”**
- Internal field binding: `webhookVerifierToken`
- Help text (small description below the field):  
  “Paste the ‘webhook verifier token’ from your Intuit Developer Webhooks page. This is used to verify that webhook calls really come from QuickBooks.”

2.3) Make this a sensitive value:
- Use a password/secret UI style (masked input) if that pattern already exists in the app (for example, similar to how API keys or other secrets are displayed).
- Ensure that the value is **not** accidentally logged to the console or client.

2.4) Ensure that saving the QuickBooks settings persists `webhookVerifierToken` to the DB model field you added in step 1. Test by changing the token in the UI and confirming it updates in the DB/config loader.

3) QUICKBOOKS WEBHOOK ENDPOINT

3.1) Create a POST endpoint at:

- URL path: `/api/quickbooks/webhooks`

Use the existing server framework and routing style of this app (Next.js route handler, Express route, tRPC HTTP endpoint, etc.). Follow the same patterns as any other webhook endpoints we already have.

3.2) Make sure this endpoint has access to the **raw request body** as a string, not just the parsed JSON. Intuit’s signature validation requires hashing the exact raw body.

- If we are using Express:
  - For this route, use `bodyParser.raw({ type: 'application/json' })` (or the equivalent) so we can get `req.body` as a Buffer.
- If we are using Next.js / App Router:
  - Use the `Request` object to read the raw `arrayBuffer()` and convert it to a string before parsing JSON.
- Adapt this to the existing stack; the key is we need both:
  - `rawBody` (string or Buffer)
  - `jsonBody` (parsed JSON)

3.3) Signature verification logic:

Implement signature verification according to Intuit’s docs:

- The Intuit Webhooks page gives us a **webhook verifier token** (this is what we’re storing in `webhookVerifierToken`).
- Incoming webhook requests include an HTTP header `intuit-signature`.
- To verify:
  1. Get the raw request body as a string: `rawBody`.
  2. Get the verifier token from `quickbooksConfig.webhookVerifierToken` (or env fallback).
  3. Compute an HMAC-SHA256 hash of `rawBody` using the verifier token as the key.
  4. Base64 encode the result.
  5. Compare this base64 string to the value in the `intuit-signature` header using a timing-safe comparison (e.g. `crypto.timingSafeEqual` in Node).
  6. If the header is missing, the config has no token, or the computed signature does not match, respond with HTTP 401 or 403 and **do not** process the event.

Example pseudo-logic (adapt this into real TypeScript/JS code using Node’s `crypto` module):

- `const rawBodyString = ...;`
- `const token = quickbooksConfig.webhookVerifierToken || process.env.QUICKBOOKS_WEBHOOK_VERIFIER_TOKEN;`
- If no token → log a warning and return 500.
- `const hmac = crypto.createHmac('sha256', token).update(rawBodyString).digest('base64');`
- `const signatureHeader = req.headers['intuit-signature'];`
- If `!signatureHeader` or mismatch → return 401.

3.4) JSON parsing and basic handling:

- After a valid signature:
  - Parse the JSON payload (e.g. `const payload = JSON.parse(rawBodyString)`).
  - QuickBooks sends one or more `eventNotifications`. At this stage, we just need to:
    - Iterate over `payload.eventNotifications` (if present).
    - For each event, log a structured message to our server logs (NOT to the browser console) that includes:
      - `entity`
      - `operation`
      - `id`
      - the `payload` part
    - This is to confirm that the webhook is wired correctly before we layer in deeper business logic.

3.5) HTTP responses:

- On successful verification and logging:
  - Return `200 OK` with a JSON body like `{ success: true }`.
- On verification failure:
  - Return `401 Unauthorized` (or `403`) with `{ success: false, error: 'invalid signature' }`.
- On missing/invalid config:
  - Return `500` with a clear error message.

4) HOOKING INTO EXISTING LOGIC (LIGHT TOUCH FOR NOW)

4.1) For now, keep the QuickBooks webhook handler’s business logic minimal:

- Do **not** yet implement complex refund or inventory updates here.
- Instead, add clearly marked TODOs where we will later:
  - Sync QuickBooks payments/refunds to our internal refund ledger.
  - Trigger updates to the Sales Orders / Returns flows.

The important part for this task is:
- Configurable verifier token in QuickBooks settings.
- Correct HMAC validation and logging.
- Stable route at `/api/quickbooks/webhooks`.

5) ENV / CONFIG NOTES

5.1) Make sure we have:

- A server-side config utility (or equivalent) that exposes `quickbooksConfig.webhookVerifierToken` everywhere we need it.
- A note in comments: to get the token, the user goes to Intuit Developer → Webhooks → “Show verifier token” and pastes that into the QuickBooks settings field in our app.

5.2) Do **not** log the actual token or the `intuit-signature` header to console in production logs. It is a shared secret.

6) FINAL CHECKLIST

After implementing:

- [ ] I can open the QuickBooks settings screen and see the new “QuickBooks webhook verifier token” field, save it, and have it persist.
- [ ] The backend can read `webhookVerifierToken` from config.
- [ ] The `/api/quickbooks/webhooks` POST route exists and is wired into the app’s routing.
- [ ] The route verifies the `intuit-signature` using the verifier token and raw body.
- [ ] Valid test webhooks from Intuit return 200 and are logged.
- [ ] Invalid signature requests are rejected with 401/403.
- [ ] No secrets are printed to logs.

Please implement all of the above, following the existing code style and patterns in this repository.
