# Changes - 2026-05-08

## Pending commit

### Route auth compatibility and reorder in-memory defaults

Files:

- `server/middleware/auth.ts`
- `server/routes.ts`
- `server/storage.ts`

What changed:

- `requireRole` now accepts both legacy array-style calls (`requireRole(["admin"])`) and newer variadic calls (`requireRole("manager")`).
- Removed the duplicate `requireRole` import in `server/routes.ts`.
- Added missing defaults in `MemStorage.createReorderAlert` and `MemStorage.createVendorCommunication` for schema fields that now have database defaults.

Why:

- The static check found a contained recent break in the route-auth surface around reorder-alert admin endpoints.
- The MemStorage fixes reduce local/test fragility around the new reorder-alert/vendor-communications tables without touching production data.

Verification:

- `npm run build` succeeds.
- The specific duplicate `requireRole` TypeScript errors are gone.

